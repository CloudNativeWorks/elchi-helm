image:
  repository: jhonbrownn/elchi
  tag: v0.1.0
  pullPolicy: Always

replicas: 3

resources:
  requests:
    memory: "255Mi"
    cpu: "200m"
  limits:
    memory: "512Mi"
    cpu: "300m"

service:
  type: ClusterIP
  port: 80
